box: ninkigumi/hugo-min
build:
  steps:
    - script:
        name: hugo
        code: |
            hugo
deploy:
  steps:
    - install-packages:
        packages: git openssh-client
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        domain: dev.ninkigumi.com
        basedir: public
