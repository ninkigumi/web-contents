box: 
  id: ninkigumi/hugo
  cmd: /bin/bash
build:
  steps:
    - script:
        name: hugo
        code: |
            hugo
deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        domain: dev.ninkigumi.com
        basedir: public
